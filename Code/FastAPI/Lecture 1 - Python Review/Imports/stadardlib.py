"""
Stadnard Library comes with Python

Useful methods!
"""

# Random
import random
types_of_drinks = ['Soda', 'Coffee', 'Water', 'Tea']
print(random.choice(types_of_drinks))

print(random.randint(1,10)) # Will print a random number between 1 and 10, if we remove import random we will get errors.

import math
square_root = math.sqrt(64)
print(square_root)

# Typically you want all imports at the beginning and ordered. 
